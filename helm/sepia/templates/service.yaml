apiVersion: v1
kind: Service
metadata:
  name: {{ include "sepia.fullname" . }}
  labels:
    {{- include "sepia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sepia.selectorLabels" . | nindent 4 }}
